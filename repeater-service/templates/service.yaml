apiVersion: v1
kind: Service
metadata:
  name: {{ include "repeater-service.fullname" . }}
  labels:
    {{- include "repeater-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repeater-service.selectorLabels" . | nindent 4 }}
